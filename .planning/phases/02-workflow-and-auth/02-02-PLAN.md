---
phase: 02-workflow-and-auth
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [INFRA-03, RESIL-04]

must_haves:
  truths:
    - "A manual workflow_dispatch run completes successfully in GitHub Actions UI"
    - "After the run, git ls-remote --tags origin shows a tag matching today's UTC date"
    - "The workflow run log shows the data branch was pushed (not just tagged)"
    - "The workflow job does not show any 403 or permission-denied errors"
    - "RESIL-04 is accepted as satisfied by built-in GitHub failure notification (no additional code)"
  artifacts:
    - path: ".github/workflows/scrape.yml"
      provides: "Live-verified workflow (created in Plan 01, verified live in this plan)"
      contains: "schedule: cron: '0 4 * * *'"
  key_links:
    - from: "GitHub Actions runner"
      to: "data branch (remote)"
      via: "git -C data-branch push origin data"
      pattern: "refs/heads/data updated"
    - from: "GitHub Actions runner"
      to: "dated tag (remote)"
      via: "git -C data-branch push origin YYYY-MM-DD"
      pattern: "refs/tags/2026-"
---

<objective>
Trigger a live workflow_dispatch run and verify that the complete workflow executes successfully — the scraper runs, commits to the data branch, and pushes a dated tag.

Purpose: INFRA-03 (commit + tag on success) and the concurrency serialization behavior of INFRA-04 cannot be verified statically. This checkpoint provides human sign-off that the workflow is correctly wired end-to-end with GITHUB_TOKEN auth in a real GitHub Actions environment.

Output: Human confirmation that the workflow is live and functional. No code changes in this plan.
</objective>

<execution_context>
@/Users/jan.prill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jan.prill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-workflow-and-auth/02-RESEARCH.md
@.planning/phases/02-workflow-and-auth/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push workflow to remote and trigger workflow_dispatch</name>
  <files></files>
  <action>
    Before triggering, confirm the workflow file is committed and pushed to the default branch (master):
    ```bash
    git status .github/workflows/scrape.yml
    git log --oneline -3
    git push origin master
    ```

    Then trigger the workflow via GitHub CLI:
    ```bash
    gh workflow run scrape.yml --repo QuantLaw/gesetze-im-internet
    ```

    If gh CLI is not authenticated, authenticate first:
    ```bash
    gh auth status
    gh auth login   # if needed
    ```

    Wait for the run to complete (poll until done):
    ```bash
    gh run list --repo QuantLaw/gesetze-im-internet --workflow=scrape.yml --limit=1
    gh run watch --repo QuantLaw/gesetze-im-internet   # follows the run live
    ```

    After the run completes, verify the tag was pushed:
    ```bash
    git ls-remote --tags origin | grep "refs/tags/$(date -u +%Y-%m-%d)"
    ```

    Expected output: a line like:
    ```
    abc123def456...   refs/tags/2026-03-15
    ```

    Also verify the data branch was updated (commit, not just tag):
    ```bash
    git ls-remote --heads origin data
    ```

    Note: If the workflow fails with a 403 error, the `permissions: contents: write` declaration may not have taken effect — check that the commit containing the YAML was pushed to master before triggering. GitHub Actions reads the workflow file from the branch that triggered it.

    Note on RESIL-04: The workflow has no `continue-on-error` on the scraper step. If the scraper exits non-zero, the job fails and GitHub's built-in notification system sends a failure email to the workflow creator (the account that last modified the cron trigger line). This is the RESIL-04 mechanism — no additional code is needed.
  </action>
  <verify>
    <automated>git ls-remote --tags origin | grep "refs/tags/$(date -u +%Y-%m-%d)"</automated>
  </verify>
  <done>The command above returns at least one line containing today's UTC date tag. The workflow run in the GitHub Actions UI shows green status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — live workflow run verification</name>
  <files></files>
  <action>Human visually verifies the live workflow_dispatch run succeeded and the data branch received a dated commit and tag. See how-to-verify below for exact steps.</action>
  <verify>
    <automated>MISSING — requires human visual confirmation of GitHub Actions UI and live remote tag check</automated>
  </verify>
  <done>Human types "approved" after confirming green run status, dated tag in data branch, and no permission errors in logs.</done>
  <what-built>
    Complete GitHub Actions workflow (Plan 01 + Task 1 above):
    - Daily cron at 04:00 UTC + manual workflow_dispatch trigger
    - GITHUB_TOKEN authentication with permissions: contents: write
    - Two-checkout pattern: source + data branch into data-branch/
    - uv + Python 3.13 setup with lockfile cache
    - Scraper invoked with --date UTC output into data-branch/
    - git commit + dated tag + push to data branch
    - Concurrency serialization: cancel-in-progress: false
  </what-built>
  <how-to-verify>
    1. Open: https://github.com/QuantLaw/gesetze-im-internet/actions/workflows/scrape.yml
       - Confirm the most recent run shows green checkmark
       - Click the run and check each step completed without errors
       - Confirm "Run scraper" step shows output from scrape.py (law count, date)

    2. Verify the dated tag was pushed to the data branch:
       ```bash
       git ls-remote --tags origin | grep "refs/tags/$(date -u +%Y-%m-%d)"
       ```
       Expected: one line with today's date tag

    3. Verify data branch received a new commit (not just a tag):
       ```bash
       git ls-remote --heads origin data
       # SHA should differ from what it was before the run
       ```

    4. Check the Actions UI shows the permission model is correct:
       - No "Permission denied" errors in the logs
       - No SSH key references in the workflow log

    5. Optional (INFRA-04 concurrency check): Fire a second workflow_dispatch while the first is still running. Observe that the second run shows "Queued" status, not "Running" or "Cancelled". This is low-priority for Phase 2 sign-off — document if not tested.

    6. RESIL-04 acceptance: Confirm in GitHub account settings that email notifications for Actions failures are enabled (Settings > Notifications > GitHub Actions). No code change needed — document acceptance.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all verifications pass.
    Or describe any issues observed (e.g., "403 on push step", "tag not found", "step X failed").
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
- All 12 automated tests pass: `uv run pytest tests/ -v`
- Live workflow_dispatch run completed green in GitHub Actions UI
- `git ls-remote --tags origin` shows today's UTC date tag in the data branch
- No 403 / permission errors in workflow logs
- Human has approved the checkpoint
</verification>

<success_criteria>
All four Phase 2 ROADMAP success criteria satisfied:
1. A workflow_dispatch run completes successfully and a new dated tag appears in the data branch
2. `git ls-remote --tags origin` shows the expected date tag after the workflow completes
3. The workflow YAML declares `permissions: contents: write` and no SSH keys or PATs are used
4. Two simultaneous workflow triggers result in sequential execution (second queues) — documented or tested
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-and-auth/02-02-SUMMARY.md` following the summary template.
</output>
